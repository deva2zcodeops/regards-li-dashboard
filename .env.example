# Copy this file to .env and fill in your values.
# .env is git-ignored — never commit real values.

# URL that the browser uses to reach the API server.
# For local dev this is typically localhost; for production use the
# server's public IP or hostname visible to end-users.
VITE_API_URL=http://localhost:3002
